for create table in h2 database
CREATE TABLE `h2database`.`userinfo` (
  `login` TINYINT NULL,
  `userinfo` VARCHAR(20) NULL,
  `name` VARCHAR(30) NULL,
  `gender` VARCHAR(6) NULL,
  `mobileno` INT(10) NULL,
  `emailid` VARCHAR(30) NULL);

For transparent stage
stage.initStyle(StageStyle.TRANSPATENT);
scene.setFill(javafx.scene.paint.Color.TRANSPARENT);

For icons
https://icons8.com/

//always stay on top of other windows
primaryStage.setAlwaysOnTop(true);

//resizable property disable
primaryStage.setResizable(false);

//transparent window
primaryStage.initStyle(StageStyle.TRANSPARENT);


//friendlist page containt
chat windows
friend list
setting
	about app
status bar


//to see memory usege
jconsole
in cmd

//learn regex
https://regexr.com/

//for chat view
https://stackoverflow.com/questions/41851501/how-to-design-chatbox-gui-using-javafx
https://www.youtube.com/watch?v=bxxebmY22E8
